import useTitle from "../../../hooks/useTitle";

 
const ShoppingCart = () => {  
    useTitle('Wheels Ville | My Bag')
    return (
        <div> 
        </div>
    );
};

export default ShoppingCart;